# Vacío = same-origin /api (proxy Vite en local, proxy Netlify en prod).
# Solo usá URL absoluta si el backend tiene tu origen en CORS_ORIGINS.
VITE_API_URL=

# Destino del proxy de Vite en desarrollo (opcional)
# VITE_API_PROXY_TARGET=https://backend-acv2.onrender.com

# ID de encuesta activa (opcional, se muestra en CommunityPage)
VITE_POLL_ID=
